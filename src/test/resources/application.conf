unLocal = true

# To comply with the Leak Detection Service, this is now provided at runtime as a command line System Property via Jenkins.
#baseUrl = "..."

services {

}

perftest {
  rampupTime = 1
  constantRateTime = 5
  rampdownTime = 1
  loadPercentage = 100
  journeysToRun = [
    upscan
  ],
  percentageFailureThreshold = 5

}