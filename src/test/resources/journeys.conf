journeys {

  clean-pdf-journey = {
    description = "Upload clean pdf"
    load = 12  // 97% of journeys per second
    parts = [
      clean-pdf
    ]
  }

  virus-journey = {
    description = "Upload virus"
    load = 0.12 // 1% of journeys per second
    parts = [
      virus
    ]
  }

  invalid-txt-filetype-journey = {
    description = "Upload invalid .txt filetype"
    load = 0.24 // 2% of journeys per second
    parts = [
      invalid-txt-filetype
    ]
  }
}
